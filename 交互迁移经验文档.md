# Metro Loom 交互迁移经验文档

这份文档的目标不是复述代码，而是把这套项目里“为什么手感好、为什么看起来灵”的交互方法，拆成可以迁移到别的项目里的执行方案。

适用场景：

- 你要把这套“手机竖屏、轻解谜、强反馈、低学习成本”的交互感迁移到另一个 React/H5 项目
- 你不一定做地铁题材，但想复用它的入场、拖拽、奖励、教程、世界地图建设感
- 你希望拿着文档就能做出“很像”的效果，而不是只得到抽象建议

参考实现主要来自：

- `src/game/CurrentGame.tsx`
- `src/styles.css`

## 1. 先记住：这套交互好看，不是因为动画多，而是因为层次清楚

它的核心不是“炫”，而是下面 6 件事同时成立：

1. 所有重要操作都只有一个主动作。
2. 每个主动作都在 150ms 到 800ms 内给出可见反馈。
3. 反馈分层，不把所有东西堆在一个动画里。
4. 新手引导不是弹窗讲解，而是直接在操作目标上画手势。
5. 奖励不是直接数字加 1，而是让奖励“飞到目标位置”。
6. UI 主体是柔和卡片，真正强烈的颜色只给可交互对象和结果。

如果你迁移时只搬视觉、不搬这 6 条，最后很容易变成“像皮肤，不像手感”。

## 2. 先拆成 6 个可迁移模块

建议把这套交互拆成下面 6 层。迁移时也按这个顺序做。

### 2.1 首屏层

职责：

- 建立题材气质
- 告诉用户“点哪里开始”
- 不解释规则，只让用户进入

当前项目做法：

- 全屏 Hero 图
- 两层雾光 + 一层地面辉光 + 两层地平线虚化
- 大标题轻微浮动
- 底部提示文案做低频呼吸
- 整个首屏只有一个动作：点击任意位置进入

关键点：

- 标题不是居中静止，而是 `-2.4deg` 轻旋转后上下浮动，视觉会更“活”
- 文案不多，只有 `kicker + title + small badge + prompt`
- 背景图上面又叠了一层很轻的暖色和冷色渐变，不直接裸图

建议你迁移时保持：

- 只保留一个 CTA：`Tap anywhere to begin`
- 标题浮动周期控制在 `3.0s - 3.4s`
- 底部提示呼吸周期控制在 `1.6s - 1.8s`
- 不要在首屏放复杂按钮组

可直接参考的参数：

- 标题浮动：`3.2s ease-in-out infinite`
- 提示呼吸：`1.7s ease-in-out infinite`

### 2.2 棋盘操作层

职责：

- 让用户通过最自然的手势完成主要玩法
- 拖拽时就预览结果，不等松手
- 支持点击、拖拽、回退、缩放、平移，但不让它们互相打架

当前项目做法：

- 轻点站点：选中
- 从站点拖到站点：铺线
- 拖拽中经过多个站点：连续施工
- 拖回上一站：回退路径
- 双指：缩放
- 空白区域拖动：平移棋盘

关键状态结构：

- `drag`
- `camera`
- `selectedStationId`
- `activeTool`
- `feedback`

迁移时建议你也保留类似的数据结构：

```ts
type DragState = {
  fromId: string;
  moved: boolean;
  pathNodeIds: string[];
  pointerId: number;
  reachableTargetIds: string[];
  startX: number;
  startY: number;
  targetId: string | null;
  x: number;
  y: number;
};

type BoardCamera = {
  centerX: number;
  centerY: number;
  zoom: number;
};
```

关键交互参数：

- 拖拽阈值：`10px`
- 路线点击取消阈值：`12px`
- 最小缩放：`0.22`
- 常规最大缩放：`5.6`
- 极限最大缩放：`22`

为什么这组参数有效：

- `10px` 足够过滤误触，但不会让拖拽显得笨
- 路线操作和棋盘平移分开识别，能避免“我想拆线却开始拖画布”
- 所有预览在拖动时就实时计算，用户不会在松手后才知道失败

迁移原则：

1. 先识别是否是双指，双指一旦成立，立即取消单指建线。
2. 单指按下站点后先进入 `drag.moved = false`。
3. 只有距离超过阈值才转为真正拖拽。
4. 拖拽过程中持续寻找最近可达目标，不要只在松手时判断。
5. 松手时优先提交“已预览过的合法路径”。

这会直接决定“顺手”还是“别扭”。

### 2.3 施工反馈层

职责：

- 用户刚开始拖，就知道这段能不能连
- 用户提交后，知道是新建、升级、删除还是失败

当前项目把反馈拆成了 4 层，而不是 1 个大动画：

1. 候选路线呼吸
2. 拖拽中的虚拟轨道预览
3. 提交瞬间的闪光/形变
4. 站点锁定脉冲

推荐你迁移时也这么分：

- `candidate`
  用于告诉用户“这里可操作”
- `preview`
  用于告诉用户“如果现在松手，会发生什么”
- `commit`
  用于告诉用户“动作已经生效”
- `targetPulse`
  用于强调目标站或目标段

建议动画时长：

- 候选呼吸：`900ms - 1100ms infinite`
- 提交闪光：`420ms`
- 升级形变：`560ms`
- 删除塌缩：`700ms`
- 目标锁定脉冲：`700ms - 760ms infinite`

当前项目的关键差异：

- 升级不是突然切换成双线，而是做一次“从偏移位置回归”的分裂动画
- 删除不是直接消失，而是“略缩 + 发光 + 塌缩”
- 候选状态只改透明度，不乱抖、不大缩放

这个克制很重要。主玩法频繁发生的反馈，越克制越高级。

### 2.4 HUD 奖励层

职责：

- 把抽象结果转成“看得见的奖励”
- 让目标系统有庆祝感

当前项目最值得迁移的是“星星飞入 HUD”。

它不是简单地把星标点亮，而是：

1. 生命值心形先变形成星星
2. 星星从来源位置起飞
3. 带一条短尾迹
4. 中途放大
5. 到目标星位时过冲
6. 最后缩回到目标大小并完成点亮

这个效果非常值钱，因为它把“剩余生命转奖励”的逻辑变成了真正可感知的因果链。

建议你迁移时保留以下机制：

- 先有来源元素变化，再发射，不要无中生有
- 飞行必须有起点和终点的真实屏幕坐标
- 动画不要直线匀速，应带中段放大和终点过冲
- 飞行完成后再点亮目标徽章

当前项目关键参数：

- 飞行总时长：`680ms`
- 飞行轨迹：起点 -> 中点放大 -> 目标上方过冲 -> 回落
- 尾迹宽度：按飞行距离动态算，不写死

建议你的通用实现：

```ts
type RewardFlight = {
  x: number;
  y: number;
  midX: number;
  midY: number;
  targetX: number;
  targetY: number;
  approachY: number;
  sourceSize: number;
  targetSize: number;
  delayMs: number;
};
```

关键不是这个类型本身，而是你必须把“奖励飞行”做成独立层，不要塞进来源节点或目标节点里硬做。

### 2.5 教程引导层

职责：

- 限制玩家只看当前要学的那一件事
- 用画面指导操作，不靠大段文字

当前项目的教程层非常值得复用，因为它不是传统遮罩弹窗，而是：

- 计算目标元素屏幕位置
- 挖出一个或多个圆角高亮孔
- 在高亮区域上画拖拽线或点击环
- 只放行当前允许操作的区域

这套方案迁移价值非常高。

你应当复用的结构：

1. `focus target`
   可以是 CSS selector，也可以是棋盘中的逻辑对象
2. `focus rect`
   根据目标实时算出屏幕矩形
3. `gesture guide`
   拖拽时是一条线 + 一个手型，点击时是一个点击环
4. `pass-through area`
   只有需要用户点的地方可以穿透

这比“全屏遮罩 + 弹窗”强很多，因为它在教真实操作路径。

迁移时必须遵守：

1. 教程只教一个动作。
2. 高亮范围必须略大于真实点击区域。
3. 手势动画应循环，但速度偏慢。
4. 用户能操作的区域必须真实可点，不要视觉上放行、实际事件被遮住。

建议参数：

- 焦点 padding：按钮 `10px` 左右，线路 `24px - 26px`
- 轻点焦点最小尺寸：`88px`
- 拖拽手势循环：`1.8s` 左右
- 点击手势循环：`1.45s` 左右

### 2.6 世界地图建设层

职责：

- 把“通关”扩展成“城市被点亮”
- 让主线进度变成持续可视建设

当前项目的世界地图不是普通关卡列表，而是“城区建设图”：

- 未完成线路是蓝图虚线
- 当前目标线路是高亮待建
- 已完成线路是实色轨道
- 线路上有持续运行的小列车
- 站点会因为是否建成、是否是目标站而改变层次
- 一个城区完成时弹出完成卡，再做一次全线 sheen 扫光

这层迁移时可以保留 4 个关键感觉：

1. 蓝图态
   还没完成，但看得见未来
2. 当前目标态
   明确告诉玩家下一步建哪里
3. 已建成态
   颜色饱满、带阴影、有列车运行
4. 区域完成态
   弹窗庆祝 + 线路扫光

关键时长：

- 建设完成弹窗：`2600ms`
- 完成后扫光：`2150ms`
- 单条线路绘制：`760ms`

建议你迁移时这样做：

- 先用静态蓝图线表示未来，不要把未开放内容完全藏掉
- 当前目标线路只强化 1 条，不要同时高亮很多条
- 列车移动用 `requestAnimationFrame` 沿 path 采样，而不是纯 CSS 假装在跑
- 轨道完成时，让“线先出现”，列车后补上

## 3. 视觉语言怎么迁移

如果你想让别的项目也“有同一种高级感”，请复制视觉语言规则，而不是直接复制颜色。

### 3.1 颜色分工

当前项目的颜色分工非常明确：

- 大背景：天空色、奶油白、暖金雾
- 容器：白色半透明玻璃卡
- 功能色：线路色、目标色
- 错误色：偏粉红红，不是纯正警报红
- 成功色：偏柔和绿，不是荧光绿

迁移建议：

- 背景永远比可操作对象更淡
- 纯高饱和色只给“轨道、星星、目标站、生命值”这种结果信号
- 错误和成功都走“奶油化”而不是企业后台风

### 3.2 体积感

项目大量使用下面 3 种东西：

- `inset highlight`
- `soft drop-shadow`
- `slight blur / backdrop-filter`

这会让 UI 看起来像玩具、徽章、糖果，而不是扁平业务系统。

迁移时的基本卡片公式：

```css
border: 1px solid rgb(255 255 255 / 82%);
border-radius: 20px;
background: linear-gradient(180deg, rgb(255 255 255 / 96%), rgb(244 248 252 / 94%));
box-shadow:
  inset 0 1px 0 rgb(255 255 255 / 94%),
  0 14px 24px rgb(48 70 95 / 11%),
  inset 0 -10px 14px rgb(207 217 228 / 12%);
backdrop-filter: blur(18px);
```

你不需要数值完全一样，但分层关系建议保持。

### 3.3 动画气质

整套项目的动画不是“快狠准”，而是“软弹、轻浮、短促地庆祝”。

通用缓动建议：

- 入场：`cubic-bezier(0.22, 1, 0.36, 1)`
- 奖励飞行：`cubic-bezier(0.24, 0.82, 0.28, 1)`
- 柔和循环：`ease-in-out`

不要大量使用：

- 机械线性位移
- 高频缩放抖动
- 超过 1 秒的主反馈动画

## 4. 如果你要在别的项目复刻，状态机至少要这样设计

不要直接从视觉做起。先把状态机搭出来。

### 4.1 页面级状态

```ts
type Screen = "splash" | "home" | "play" | "pause" | "victory" | "failure";
```

### 4.2 交互级状态

```ts
type InteractionState = {
  camera: { centerX: number; centerY: number; zoom: number };
  drag: DragState | null;
  selectedNodeId: string | null;
  activeTool: "erase" | "hint" | "heal" | "safeErase" | null;
  feedback: {
    nonce: number;
    text: string;
    tone: "idle" | "move" | "error" | "success";
  };
};
```

### 4.3 引导级状态

```ts
type TutorialState = {
  step: string;
  completed: boolean;
  checkpoint: object | null;
};
```

### 4.4 奖励级状态

```ts
type RewardUiState = {
  lifeLossPulseNonce: number;
  goalFlights: RewardFlight[];
  completionBurstNonce: number;
  showCompletionSheen: boolean;
};
```

为什么要分这么细：

- 页面级控制“在哪一屏”
- 交互级控制“手现在在做什么”
- 引导级控制“现在允许学什么”
- 奖励级控制“反馈动画发不发”

如果你把这些混成一个大状态对象但没有分层思维，后面非常容易互相污染。

## 5. 最值得直接抄走的实现套路

### 5.1 奖励飞行一定用“独立浮层”

不要把星星动画绑在来源节点里做，也不要绑在 HUD 里做。

正确做法：

1. 读取来源 DOMRect
2. 读取目标 DOMRect
3. 折算到同一层坐标系
4. 在 `position: absolute` 的 flight layer 里创建一个临时 flying node
5. 动画结束后销毁 flying node

这是这套项目最有价值的交互工程套路之一。

### 5.2 教程焦点一定用“计算矩形 + 镂空遮罩”

不要写死引导位置。

正确做法：

1. 根据 selector 或逻辑对象实时求 rect
2. 生成圆角路径
3. 遮罩外层拦截事件
4. 只对可交互的焦点区放行

这样页面重排、不同手机尺寸、语言切换都不会轻易把教程搞坏。

### 5.3 拖拽预览一定早于提交

不要等用户松手后才告诉他失败。

正确做法：

1. 拖动时实时寻找合法目标
2. 实时模拟动作结果
3. 实时切换预览颜色和形态
4. 松手时优先提交最后一次合法结果

这能直接提升“手感”。

### 5.4 高频反馈只改 1 到 2 个维度

候选态只改透明度。

错误态只改底色和文案色。

目标站强调主要靠脉冲和发光，不做乱飞乱跳。

这会让项目长期玩起来不累。

## 6. 推荐的迁移顺序

如果你要把这套效果迁到别的项目，不要一次性全搬。按下面顺序。

### 第一阶段：先复刻手感，不复刻美术

1. 搭 `splash / home / play`
2. 搭拖拽、缩放、平移冲突处理
3. 搭施工预览和提交反馈
4. 搭顶部 HUD 和底部工具栏

阶段验收标准：

- 用户不看说明，也能自然理解“拖拽完成主要动作”

### 第二阶段：再复刻奖励链路

1. 生命值或资源徽章
2. 奖励飞行到 HUD
3. 关卡完成卡
4. 失败卡和受伤闪屏

阶段验收标准：

- 用户能明显感受到“我为什么得星、为什么扣血”

### 第三阶段：最后复刻世界建设层

1. 蓝图线
2. 已建线
3. 线路上小列车
4. 区域完成弹窗和 sheen

阶段验收标准：

- 用户会觉得自己是在“建设世界”，不是在刷关卡编号

## 7. 迁移时最容易踩的坑

### 坑 1：动画很多，但没有因果链

错误做法：

- 点一下弹 3 个东西
- 但用户不知道哪一个对应自己的操作

正确做法：

- 每个反馈都能对应到一个输入或一个结果

### 坑 2：教程只讲，不演示

错误做法：

- 弹窗写“请拖动节点连接路线”

正确做法：

- 直接高亮两端站点并画拖动手势

### 坑 3：想用 CSS 强行做所有运动

错误做法：

- 列车沿路径移动也纯 CSS 蒙混

正确做法：

- 路径相关运动用真实 path 采样

### 坑 4：复制视觉，不复制状态分层

错误做法：

- UI 看起来像，但一拖就乱、一缩放就冲突

正确做法：

- 先复制交互状态机，再复制皮肤

### 坑 5：把所有高亮都做得很强

错误做法：

- 所有按钮都发光
- 所有路线都脉冲

正确做法：

- 只有“下一步应该点什么”最亮

## 8. 如果要做到“七成像”，最少保留这 10 条

1. 首屏只有一个动作。
2. 顶部 HUD 用玻璃卡，不用硬边矩形。
3. 主玩法是拖拽，不是先选工具再多步确认。
4. 拖拽中必须有实时预览。
5. 成功、错误、普通提示三种 callout 颜色必须区分。
6. 奖励必须飞入目标，而不是直接数字变化。
7. 新手引导必须直接画手势路径。
8. 世界地图必须有蓝图态和已建成态。
9. 完成一个区域时必须有一次短庆祝，而不是静默切换。
10. 高频动画必须柔和、短、克制。

## 9. 如果你要做到“九成像”，还要加上这些细节

1. 标题和徽章都带一点玩具感阴影。
2. 线路完成后列车持续运行，世界才会“活”。
3. 生命值不是数字，而是有实体心形徽章。
4. 扣血要有边缘泛红的 vignette。
5. 奖励到达 HUD 时要过冲，不要直接停。
6. 当前目标只高亮一个，避免视觉分裂。
7. 不同屏共享同一种圆角卡片语言和高光逻辑。

## 10. 建议你在新项目里直接建立的设计 token

```css
:root {
  --fx-ease-out-pop: cubic-bezier(0.22, 1, 0.36, 1);
  --fx-ease-flight: cubic-bezier(0.24, 0.82, 0.28, 1);
  --fx-duration-fast: 220ms;
  --fx-duration-commit: 420ms;
  --fx-duration-upgrade: 560ms;
  --fx-duration-remove: 700ms;
  --fx-duration-reward-flight: 680ms;
  --fx-duration-panel-celebrate: 2600ms;
  --fx-duration-sheen: 2150ms;
  --fx-radius-card: 20px;
  --fx-radius-pill: 999px;
  --fx-shadow-card: 0 14px 24px rgb(48 70 95 / 11%);
  --fx-shadow-float: 0 8px 14px rgb(255 192 51 / 18%);
}
```

这些 token 不要求名字一样，但最好先抽出来。这样你迁移到任何项目时，能先把“气质”定住。

## 11. 最后给你的落地建议

如果你真要迁移，不要问“怎么复制这个页面”，而要按下面问题推进：

1. 这个新项目的“唯一主动作”是什么。
2. 这个主动作在按下、拖动、提交、成功、失败时，分别应该出现什么反馈。
3. 奖励从哪里来，要飞到哪里去。
4. 新手第一次进入时，只需要学哪一个动作。
5. 世界层的进度应该如何被持续可视化。

只要这 5 个问题答得清楚，你就能把 Metro Loom 的交互感迁移过去，而且不会只剩下表面样式。

如果需要继续做下一步，建议直接做其中一种：

- 我根据你的目标项目，给你出一版“迁移实施清单”
- 我把这份文档进一步改成“React 组件拆分方案”
- 我直接给你抽一套可复用的交互基础组件接口
